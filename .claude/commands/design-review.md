Review the current UI against the Design System Law in `CLAUDE.md` and Sections 3–4
of `90s-geometric-color-system-strategy.md`.

For each violation, report:
- **The rule** it breaks (name it — e.g. "Rule 6, keyline law" or "Shape grammar: corners").
- **The offending element/file** (selector + `file:line`).
- **The fix** (concrete: the token, stroke, corner, or shadow to change).

Rank findings by visual impact (most jarring first). Then **implement the top 3**
fixes and tell me exactly what to check at `/styleguide/` and on the live pages.
